---
import MemberLayout from "../../layouts/MemberLayout.astro";
import { postsByAuthor } from "../../data/blog";
import { getPersonBySlug } from "../../data/people";

const person = getPersonBySlug("mac")!;
---

<MemberLayout person={person} posts={postsByAuthor(person.name)} />
